# FRECA Task 2 — 整体方案选型

> 版本：v5.0 · 2026-07-17
> 目的：给出有名字的、可识别的整体方案，每个方案说清优缺点和与赛题的结合性
> 选型维度：准确率 / 速度 / 成本 / 合规 / 工程量

---

## 方案 1：**RAG + 单 LLM（Naive RAG）**

### 一句话
把所有相关证据切片塞进 prompt，让 LLM 一次推理。

### 工作流
```
[政策 PDF 切块] → 向量化 → 存 ChromaDB
[9 份证据切块] → 向量化 → 存 ChromaDB
                              ↓
                   [检索 top-k 相关片段]
                              ↓
                   [prompt: 政策 + 证据 + CP]
                              ↓
                   [LLM 一次推理 → 1/0/N/A]
```

### 优点
- 实现最简（1-2 天）
- 调试容易
- token 可控

### 缺点
- 准确率上限低（检索质量决定一切）
- 多模态弱
- 不擅长多跳推理

### 与赛题结合性
- ✅ 合规（不硬编码规则）
- ✅ 满足多模态（图像直传）
- ⚠️ 准确率可能不够

---

## 方案 2：**LangGraph ReAct Agent（Agentic RAG）**

### 一句话
LLM 作为 Agent，自主决定"读哪个证据 / 查哪条政策 / 调哪个工具"。

### 工作流
```
[LLM Agent]
   ↓ 思考：要审计 CP17
   ↓ 行动：调用 read_track(3)
   ↓ 观察：拿到 Pest Activity Log
   ↓ 思考：需要查政策
   ↓ 行动：调用 search_policy("CP17")
   ↓ 观察：拿到 Rules s4-7A 段落
   ↓ 思考：交叉对比 Track 4
   ↓ 行动：调用 read_track(4)
   ↓ 综合判断 → 裁决
   ↓ 重复 3-10 步
```

### 优点
- 推理可解释（每步可见）
- 自主决策，能处理复杂 case
- LangGraph 提供成熟框架

### 缺点
- 慢（多轮调用）
- 调试困难（agent 可能陷入循环）
- token 消耗高

### 与赛题结合性
- ✅ 合规（工具不预存规则）
- ✅ 多模态（工具可传图）
- ✅ 适合异常 case（自主决策）
- ⚠️ 96 case 全跑成本高

---

## 方案 3：**Multi-Agent Debate（多角色辩论）**

### 一句话
3 个 LLM 角色（支持合规 / 支持违规 / 终审法官）辩论投票。

### 工作流
```
[Auditor 主] → 给出初判
   ↓
[Proponent] vs [Opponent] → 辩论 3 轮
   ↓
[Judge] → 综合双方意见 → 终裁
```

### 优点
- 准确率最高（多视角）
- 偏见少
- 可解释性极强

### 缺点
- 极慢（3-5x 调用）
- 极贵（3-5x token）
- 96×41 全跑不现实

### 与赛题结合性
- ✅ 合规
- ⚠️ 仅适合 LOW trust_score 的少数 case
- ❌ 不适合全 96 case

---

## 方案 4：**Two-Stage Pipeline（两阶段流水线）**

### 一句话
**预审（纯本地）→ 裁决（LLM）**，分两步走。

### 工作流
```
[Stage 1-6: 纯本地]
  - 解析证据
  - 筛异常 case
  - 算 trust_score
  - 列 known_issues
   ↓
[Stage 7: LLM 裁决]
  - 输入: 证据 + policy + integrity summary
  - 输出: 41 个裁决
```

### 优点
- 异常 case 提前剔除，省 LLM 调用
- 预审结果可作为 LLM 上下文
- 工程化最好（checkpoint + 状态机）

### 缺点
- 预审脚本需要开发
- 两阶段衔接的契约要设计

### 与赛题结合性
- ✅ **最契合**（预审解决"农场提交不规范"）
- ✅ 合规
- ✅ 多模态（Stage 7 仍可传图）
- ✅ 工程化最强（可中断/可恢复）

---

## 方案 5：**RAG + Fine-tuning（检索增强微调）**

### 一句话
先用大模型跑全 96 case 产生伪标签，再微调小模型本地推理。

### 工作流
```
[Opus] → 跑 96 case → 产生 3,936 伪标签
   ↓
[用伪标签微调 Qwen2.5-7B]
   ↓
[Qwen2.5-7B 推理] → 本地 + 快
```

### 优点
- 推理成本降 10-50x
- 可本地部署
- 推理快

### 缺点
- 需要标注数据（伪标签质量决定上限）
- 训练成本高（A100 × 4 跑 1 天）
- 准确率受限于教师模型

### 与赛题结合性
- ⚠️ **依赖伪标签质量**，无真实标注
- ❌ 工程量最大
- ❌ 时间紧迫不现实

---

## 方案 6：**Constrained Decoding + Self-Consistency（约束解码 + 自一致性）**

### 一句话
用 schema 强制输出格式，同一 CP 调 3 次投票。

### 工作流
```
[LLM 推理] → [约束层: 必须是 1/0/N/A]
                          ↓
                    [输出合法]
[同 CP 重复 3 次] → [投票多数]
```

### 优点
- 输出格式 100% 合规（无需 retry）
- 准确率比单次推理高 3-5%
- 实现简单（套 schema）

### 缺点
- 成本 3x（同 CP 调 3 次）
- 偶发问题：3 次都错时无救

### 与赛题结合性
- ✅ 合规
- ✅ 工程化好
- ⚠️ 成本 3x = 11,808 次调用

---

## 方案 7：**Knowledge Graph + GraphRAG（知识图谱）**

### 一句话
从政策 PDF 抽取 RE-CP-Rule 三元组，构建图谱做推理。

### 工作流
```
[政策 PDF] → LLM 抽取 → 三元组
   ↓
[Neo4j / NetworkX 图]
   ↓
[查询: "case 35 的 CP17 是否满足?"]
   ↓
[Cypher / SPARQL 推理]
   ↓
[裁决]
```

### 优点
- 推理可解释
- 支持复杂查询
- 知识沉淀

### 缺点
- **赛题禁止"硬编码规则到 prompt"，KG 抽取本质上是把规则结构化**
- 图构建成本高
- 更新困难

### 与赛题结合性
- ❌ **合规风险最高**（可能被裁定违规）
- ⚠️ 仅在组委会明确 OK 时才用

---

## 方案 8：**Hybrid: Two-Stage + Agentic RAG（混合方案）**

### 一句话
**预审筛异常 → 简单 case 用 RAG → 复杂 case 用 LangGraph Agent**。

### 工作流
```
[Stage 1-6: 预审] → trust_score
   ↓
[HIGH case: 方向 1 RAG]
   ↓
[MEDIUM case: 方向 1 RAG + 详细 prompt]
   ↓
[LOW case: 方向 2 LangGraph Agent 自主决策]
```

### 优点
- 各取所长
- 成本可控（Agent 只跑少数 case）
- 准确率最高

### 缺点
- 实现复杂
- 两套推理逻辑要维护

### 与赛题结合性
- ✅ **最契合**（按 case 难度分级）
- ✅ 合规
- ✅ 多模态
- ⚠️ 工程量大

---

## 📊 8 个方案横向对比

| 方案 | 准确率 | 速度 | 成本 | 合规 | 工程量 | 推荐场景 |
|---|---|---|---|---|---|---|
| 1 RAG + 单 LLM | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ✅ | ⭐ | MVP / 快速验证 |
| 2 LangGraph Agent | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ✅ | ⭐⭐⭐ | 复杂 case |
| 3 Multi-Agent Debate | ⭐⭐⭐⭐ | ⭐ | ⭐ | ✅ | ⭐⭐⭐⭐ | 高价值决策 |
| **4 Two-Stage Pipeline** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ✅ | ⭐⭐ | **推荐** |
| 5 RAG + 微调 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⚠️ | ⭐⭐⭐⭐⭐ | 长期 / 有标注 |
| 6 Constrained + Self-Con | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ✅ | ⭐⭐ | 准确率优先 |
| 7 Knowledge Graph | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ❌ | ⭐⭐⭐ | 不可用 |
| **8 Hybrid** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ✅ | ⭐⭐⭐⭐ | **最优** |

---

## 🎯 我的推荐

### 主推：**方案 4（Two-Stage Pipeline）**

理由：
- **最契合赛题**（"农场提交不规范" → 预审流程）
- 工程化最好（checkpoint / 状态机 / 可中断）
- 准确率、成本、工程量平衡最好
- 5 天可完成

### 升级路径：**方案 8（Hybrid）**

MVP 跑完后，如果准确率不够：
- HIGH/MEDIUM case 保持方案 1（RAG）
- LOW case 升级到方案 2（LangGraph Agent）
- 这就是方案 8 的本质

### 不推荐

| 方案 | 不推荐理由 |
|---|---|
| 3 Multi-Agent Debate | 太贵 |
| 5 RAG + 微调 | 没标注数据 + 时间不够 |
| 6 Constrained + Self-Con | 成本 3x，先不开 |
| 7 Knowledge Graph | 合规风险 |

---

## 请您选择

请告诉我从哪个方案开始：

- **A**：方案 4（Two-Stage Pipeline）—— **推荐**
- **B**：方案 8（Hybrid，预审 + 按难度分级）
- **C**：方案 1（RAG + 单 LLM）—— 最简
- **D**：方案 2（LangGraph Agent 全跑）
- **E**：其他 / 组合

收到您的选择后，我立即开始实现。