# FRECA Task 2 — 比赛介绍、数据现状与数据清洗说明

> 文档目的：给项目组新成员/协作者一份"读完就懂"的总览。
> 内容分三部分：① 比赛讲了什么 → ② 我们现在手里有哪些数据 → ③ 我们打算怎么清洗这些数据。
> 不涉及模型/框架等技术选型，只讲"任务本身"和"数据本身"。
> 版本：2026-07-18

---

## 一、这个比赛是什么

### 1.1 一句话概括

**FRECA（Farm Registered Establishment Compliance Audit，农场注册机构合规性审计）** 是第 17 届国际网络安全与生成式 AI 竞赛（CyberAI Cup 2026）中的 Task 2。任务是：**扮演监管审计员，对一批澳大利亚农业出口企业做合规性审核，给出每个检查点"合规 / 不合规 / 不适用"的判定。**

### 1.2 业务背景

澳大利亚的农业出口商必须通过"注册机构（Registered Establishment，简称 RE）"来经营，且必须符合《2021 年出口管制（植物及植物产品）规则》（下称"《规则》"）。监管审计员会按一套结构化的检查标准，逐条核查每个注册机构是否达标。

这套检查标准覆盖 **4 大合规要素**，细分为 **41 个检查点（CP1–CP41）**：

| 要素 | 名称 | 检查点范围 | 关注点 |
|---|---|---|---|
| Element 1 | 注册与文档记录 | CP1–CP7 | 是否在注册范围内经营、场地规划图、变更审批 |
| Element 2 | 物理基础设施 | CP8–CP16 | 建筑/设备/照明/洗手/防虫/废物/检验区/筛选 |
| Element 3 | 运营控制 | CP17–CP28 | 卫生与废物管理、害虫防治、≥2年记录、化学品储存 |
| Element 4 | 可追溯性与安全 | CP29–CP41 | 端到端追溯、植物检疫安全、追溯记录、进口国要求 |

### 1.3 我们要做的事

对**每一个农场案例**，都要审核全部 41 个检查点，每个检查点给出一个裁决：

- **`1`（Compliant / 合规）**：证据满足该检查点。
- **`0`（Non-compliant / 不合规）**：证据不满足或与检查点相矛盾。
- **`N/A`（Not applicable / 不适用）**：该检查点不适用于这个农场的注册业务。

判断依据只有两样东西：
1. **政策文件**（《规则》全文 PDF）；
2. **农场提交的证据材料**（每个案例 9 份文件，见第二部分）。

### 1.4 评分方式

所有评分都基于 **4,100 个审计判定**（41 个检查点 × 100 个案例）计算，分三个层级：

| 评分层级 | 含义 |
|---|---|
| 整体准确率（Overall） | 全部 4,100 个判定里，判对的比例 |
| 检查点准确率（×41） | 每个检查点在所有案例上的判对比例 |
| 要素准确率（×4） | 每个合规要素内部的判对比例 |

### 1.5 一条硬性规则（很重要）

**不允许把合规规则直接写进提示词。** 例如禁止写"CP3 要求做到 X"这种硬编码。系统必须**完全依据政策文件 + 农场证据**自行推理得出结论。方法验证阶段要提交实际用过的提示词和模型版本，无法复现的提交会被判无效。

> 这条规则决定了一件事：**规则逻辑不能预先塞进代码/提示里，只能让模型现场从政策和证据里推。** 清洗数据时也要守住这条线——我们只做"整理证据"，不做"替模型下结论"。

---

## 二、现在手里有哪些数据

### 2.1 数据总览

数据放在 `D:\Data\Desktop\contest\Task2\` 下。核心文件如下：

| 文件 / 目录 | 内容 | 说明 |
|---|---|---|
| `SFRE_cases.zip` | 全部案例的证据包 | 解压后在 `extracted/SFRE_cases/` |
| `extracted/SFRE_cases/` | 已解压的案例目录 | 99 个 RE 目录，实际承载 **100 个逻辑案例** |
| `checkingpoints_all_elements_onesheet.xlsx` | CP1–CP41 完整定义 | 每个检查点的原文，是判定的标准依据 |
| `submission_template.xlsx` | 提交模板 | **目前只有一行表头，没有数据行** |
| `1-Export Control ... Rules 2021.pdf` | 政策依据全文 | 约 132 页，唯一的规则来源 |
| `Task2 Description.docx` / `task.md` | 任务说明书 | 两者内容基本一致 |
| `case_to_re.json` | case_id ↔ RE 编号映射 | 位于 `extracted/SFRE_cases/` 内 |

### 2.2 一个案例长什么样：9 份证据

每个农场案例都由农场提交 **9 份证据文件（Track 1–9）**。一个典型案例目录（如 `RE-NSW-2020-0033`）内文件如下：

| Track | 名称 | 格式 | 内容 |
|---|---|---|---|
| Track 1 | 企业登记申请表 | `.docx` | 注册主体、注册状态（current/lapsed/suspended…） |
| Track 2 | HACCP 计划 | `.docx` | 危害分析与关键控制点计划 |
| Track 3 | 害虫防治记录 | `.xlsx` | 多 sheet：封面 / 害虫活动日志 / 诱饵站登记 / 化学品储存 / 现场条件 |
| Track 4 | 农场管理计划 | `.docx` | 管理制度，内嵌引用旧版规则章节 |
| Track 5 | 农场用地规划图 | `.docx` | 场地布局、设施位置 |
| Track 6 | 农场卫生与清洁计划 | `.docx` | 卫生制度、记录留存期 |
| Track 7 | 诱饵站位置图 | `.docx` | 图形为主 |
| Track 8 | 植物检疫安全程序 | `.docx` | 防污染、防替换、包装安全 |
| Track 9 | 可追溯性记录 | `.xlsx` | 接收/搬运/处理/发运记录 |

**判定一个检查点时，往往要跨多份证据交叉阅读。** 且证据本身可能不完整、含糊、甚至互相矛盾——这是刻意设计的，模拟真实审计场景。

### 2.3 数据规模（实测）

- **逻辑案例数**：100 个（case_id 1..100 全部存在）。
- **RE 目录数**：99 个（比案例数少 1，原因见下）。
- **证据文件总数**：898 个（698 个 docx + 200 个 xlsx）。
  - 理论上应是 100 × 9 = 900 份，缺 2 份是因为有 2 个案例缺 Track 1。

### 2.4 数据里已知的"坑"（务必知道）

这批数据不是干净的。经过逐文件核查，发现下面这些结构性/内容性问题。**它们大部分是刻意设计的"农场提交不规范"，不是简单的 bug**，需要在清洗阶段专门处理：

#### （1）案例数 96 vs 100 的表面矛盾 —— 已解释清楚

任务书 "Dataset" 部分写"Total cases: 96"，但提交格式和评分部分都按 100 例、4,100 判定算。核查后确认：

> **100 个数据案例 − 4 个"提交不规范"的异常案例 = 96 个正式评测案例。**

96 不是过时数字，而是"剔除异常后的有效评测数"。这 4 个异常案例是 **case 24、35、80、100**（见下）。

#### （2）一个目录塞了两个案例

`RE-WA-2021-0077` 这一个目录里放了 18 个文件——同时容纳 **case 35（Goldfields Grain Storage）** 和 **case 100（Midwest Grain Holdings）**，两个独立案例共用同一个 RE 编号。这是 RE 目录数（99）比案例数（100）少 1 的原因。

#### （3）两个案例缺 Track 1

- **case 24**（`RE-SA-2021-0066`）缺企业登记申请表；
- **case 80**（`RE-QLD-2022-0077`）缺企业登记申请表。

且这两个案例的 Track 2（HACCP）内部都引用了不规范的注册号 `RE-QLD-2891`，是从同一个模板克隆出来的文本。

#### （4）所有 Track 3 的封面 RE 编号都对不上

100 个案例的 Track 3（害虫防治记录）封面里写的 RE 编号，**100% 与所在目录的 RE 编号不一致**。这是批量套模板生成时没改封面留下的痕迹——数据本身（活动日志、诱饵站等）是真实完整的，只是封面 RE 字段错位。**归属案例时一律以目录 RE 为准，忽略文件内 RE。**

#### （5）Track 4 用的是旧版规则编号

Track 4（农场管理计划）里引用规则时用的是 `C1..C7`（旧版编号），而官方体系是 `CP1..CP41`。二者需要建映射对照。

#### （6）证据里内嵌了"近似结论"字段

Track 3 里有一些字段几乎直接给出了审计定性，例如：
- 封面的 `Audit scenario:`（审计场景），已把案例分成 8 类典型场景（诱饵站空/损坏、记录不足2年、化学品未上锁、持续鼠患、虫害活跃、越南语记录、无害虫防治系统、完全合规）。
- 记录段的 `NOTE:` 行，如"Records from 2025 (< 2 years — NON-COMPLIANT (C3))"。

Track 1 也直接声明了注册状态（current / lapsed / suspended / under review）。

> **处理原则**：这些字段位于证据正文之中，可以作为证据的一部分让模型读取，但**不能**在提示词里直接把它们当成答案映射到 CP 编号——那样就违反了"不硬编码规则"的红线。正确做法是让模型自主推理，再拿这些字段做交叉验证。

#### （7）跨文档信息不一致

同一个案例里，不同文件之间的场名、商品、地址经常对不上；有的案例 Track 4 说"有完整 IPM 害虫防治系统"，Track 3 却说"无正式害虫防治系统"——同一事实给出互相冲突的陈述。这些正是真实审计里"材料完整性存疑"的信号。

### 2.5 提交格式

用 `submission_template.xlsx` 提交，表格结构：
- 1 行表头 + 100 行数据（每行一个案例）；
- 列为：`RE Number, CP1, CP2, …, CP41`（共 42 列）；
- 单元格只能填 `1` / `0` / `N/A`；
- **不能增删或重排行列**，RE 编号列必须与给定标识符完全一致。

> 注意：官方模板目前**只有表头、没有 100 行数据**，需要我们自己按 case_id 顺序补齐 100 行。

---

## 三、我们的数据清洗过程

清洗的核心目标：**把这批"不规范"的原始证据，整理成模型可以稳定读取、且不越过合规红线的输入**。清洗只做"整理和标注证据完整性"，**不替模型下任何 CP 结论**。

整个过程分下面几步。

### Stage 0 — 案例组装（把散落的文件按案例拼起来）

**问题**：文件名编号方式不统一，还有一个目录混装了两个案例。

**做法**：
- Track 1/2/3 从文件名里的 `_<编号>_` 取 case_id；
- Track 8/9 从文件名末尾的 3 位数字取 case_id；
- Track 4/5/6/7 文件名没有编号，用目录推断；
- 对混装的 `RE-WA-2021-0077`，用文件名里的公司名（`Goldfields` / `Midwest`）把 18 个文件拆成 case 35 和 case 100 两套。

**产出**：`case_id (1..100) → {目录 RE, 9 个文件路径}` 的完整映射，并断言恰好 100 个案例。

### Stage 0.5 — 异常案例筛选（找出"提交不规范"的案例）

**问题**：有些案例因为结构性缺陷无法正常审计，需要剔除出正式评测。

**判据**（命中任一即标为异常）：
1. **RE_CONFLICT**：同一目录出现 ≥2 个案例（→ 命中 case 35、100）；
2. **T1 缺失 + 内部不一致**：缺 Track 1，且 Track 2 内部出现不规范注册号/三重不一致（→ 命中 case 24、80）。

**产出**：`anomaly_report.json`，标记出 **4 个异常案例：case 24、35、80、100**。100 − 4 = 96，与任务书完全吻合。这 4 个案例在提交表里仍占位，41 个检查点统一填 `N/A`。

### Stage 1 — 身份一致性核查

**问题**：文件内的 RE 编号、公司名经常和目录对不上。

**做法**：核对目录 RE 与 Track 1 的 RE 是否一致、Track 3/5/9 内部 RE 是否与目录一致。对于已知的模板生成痕迹（如 Track 3 封面 RE 100% 错位），**标注 mismatch 但不阻断**，统一"以目录 RE 为准"。

**产出**：每个案例的 `identity_flags`（OK / MISMATCH）。

### Stage 2 — 证据齐全性核查

**做法**：检查每个案例 9 个 Track 是否齐全，缺哪个记录下来。

**产出**：每个案例的 `missing_tracks`（缺失的 Track 编号列表）。

### Stage 3 — 注册状态抽取

**问题**：CP1/CP2 直接依赖"机构当前是否在合法注册状态"。

**做法**：从 Track 1 抽取注册状态文字（current / lapsed / suspended / under review / pending）。Track 1 缺失时，尝试从 Track 2 推断，否则标 UNKNOWN。

**要点**：Track 1 不是"被审计的证据"，而是"审计员拿到的注册基线"——它定义机构当前合法状态，其余 Track 用来判断"在此状态下是否达标"。

**产出**：每个案例的 `reg_status`。

### Stage 4 — 跨文档矛盾检测

**做法**：检测同一案例内部对同一事实的多重陈述是否冲突，重点看三类：
- 系统存在性（有 IPM / 无 IPM）；
- 记录完整性（≥2 年 / 仅 6 个月）；
- 记录语言（English / Vietnamese）。

**产出**：每个案例的 `contradictions` 列表（哪两份文件、什么主题、冲突片段）。

### Stage 5 — 提示字段抽取（作为证据，不作为答案）

**做法**：把 Track 3 的 `Audit scenario:`、`NOTE:` 行，以及 Track 1 的注册状态文字抽出来，**作为证据的一部分**供后续推理读取。

**红线**：绝不在提示词里把这些字段直接映射成 CP 结论（如"NOTE 说 NON-COMPLIANT → CP23=0"）。正确用法是让模型独立推理后，用这些字段做交叉验证——一致则提升置信度，不一致则进一步分析。

**产出**：每个案例的 `hints`（场景、记录留存说明、注册状态文字）。

### Stage 6 — 单案例证据完整性总评

**做法**：整合 Stage 0.5–5 的结果，给每个案例打一个可信度 `trust_score`（HIGH / MEDIUM / LOW），并列出"已知问题清单"。

**产出**：每个案例的 `integrity_report`，作为后续正式审计时的上下文。

| trust_score | 含义 |
|---|---|
| HIGH | 9 份证据一致、缺漏少 |
| MEDIUM | 有 1–2 处不一致，需在裁决时说明依据来源 |
| LOW | 关键证据缺失或严重矛盾 |

### 辅助整理 — Track 4 旧版编号映射

把 Track 4 内嵌的旧编号翻译成官方 CP 编号，仅用于交叉验证（属于"数据内嵌引用的翻译"，不引入额外规则）：

| Track 4 内嵌 | 官方要素 | 官方 CP |
|---|---|---|
| C1, C2, C3 | Element 2.1 | CP8, CP9, CP10 |
| C4, C5, C6 | Element 2.2 | CP11, CP12, CP13 |
| C7 | Element 3.1 | CP17, CP18, CP19 |

### 清洗产出物汇总

| 产出 | 内容 |
|---|---|
| `anomaly_report.json` | 100 个案例的异常标记 + 原因 + 裁决占位 |
| `skeleton_submission.xlsx` | 100 行 × 41 列的提交骨架，异常案例填 N/A，其余留空 |
| 每案例 `integrity_report` | 身份一致性、缺失 Track、注册状态、矛盾清单、提示字段、可信度 |

---

## 四、一句话总结

- **比赛**：扮演审计员，依据政策 + 农场证据，对 100 个案例 × 41 个检查点各给出 `1/0/N/A`。
- **数据**：100 个案例、每案例 9 份证据（共 898 份文件），外加政策 PDF 和检查点定义；数据刻意做得"不规范"，含 4 个异常案例、模板错位、旧版编号、内嵌提示等坑。
- **清洗**：先把文件按案例拼齐 → 筛出 4 个异常案例 → 逐案核查身份/齐全性/注册状态/矛盾 → 把内嵌提示当证据抽出（不当答案）→ 给每个案例打可信度并列已知问题，最终产出可直接喂给审计流程的干净输入。
